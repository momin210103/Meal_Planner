import React from 'react';
import Hero from './Hero';

const Home = () => {
    return (
        <main className="min-h-screen w-full overflow-x-hidden">
            <Hero />
        </main>
    );
};

export default Home;